import styled from "styled-components";

const AttributeStyled = styled.a`
	position: fixed;
	bottom: 1rem;
	left: 0;
	background: rgb(0 0 0 / 20%);
	color: #fff;
	padding: 1rem;
	padding-left: 2rem;
	text-decoration: none;
`;

export {AttributeStyled}